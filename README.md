# DC 크롤러 자동화 시스템

디씨인사이드 "특이점이 온다" 갤러리를 자동으로 크롤링하여 JSON 데이터로 저장하는 시스템입니다.

## 🎯 주요 기능

- **자동 크롤링**: GitHub Actions를 통해 6시간마다 자동 실행
- **증분 저장**: 기존 데이터에 새 게시글만 추가 (중복 제거)
- **주차별 파일 관리**: ISO 8601 표준 주차로 파일 분리 (예: `2026_W05.json`)
- **완전한 데이터 수집**: 제목, 작성자, 날짜/시간, 조회수, 추천수, 댓글수 등 모든 정보 포함

## 📊 수집 데이터

각 게시글마다 다음 정보를 수집합니다:

| 필드 | 설명 | 예시 |
|------|------|------|
| `post_id` | 게시글 번호 | `"12345"` |
| `title` | 제목 | `"AGI 언제 올까요?"` |
| `author` | 작성자 닉네임/IP | `"홍길동"` 또는 `"ㅇㅇ(123.45)"` |
| `author_type` | 작성자 유형 | `"member"`, `"ip"`, `"semi_anonymous"` |
| `author_ip` | IP 정보 (있는 경우) | `"123.45"` |
| `date` | 날짜 | `"2026-01-31"` |
| `time` | 시간 | `"14:30:25"` |
| `datetime` | ISO 8601 타임스탬프 | `"2026-01-31T14:30:25"` |
| `views` | 조회수 | `1234` |
| `likes` | 추천수 | `56` |
| `comments` | 댓글수 | `23` |
| `url` | 게시글 URL | `"https://gall.dcinside.com/..."` |

## 📁 데이터 구조

```
dc_crawler/
├── data/                      # 수집된 데이터 (Git에 포함)
│   ├── 2026_W04.json         # 2026년 4주차
│   ├── 2026_W05.json         # 2026년 5주차
│   └── ...
├── crawl_dc.py               # 크롤러 메인 스크립트
├── requirements.txt          # Python 패키지 목록
└── .github/workflows/crawl.yml  # GitHub Actions 설정
```

### JSON 파일 형식

```json
{
  "gallery_id": "thesingularity",
  "gallery_name": "특이점이 온다",
  "week": "2026_W05",
  "week_start": "2026-01-27",
  "week_end": "2026-02-02",
  "last_updated": "2026-01-31T14:49:51+09:00",
  "total_posts": 150,
  "posts": [
    {
      "post_id": "12345",
      "title": "게시글 제목",
      "author": "닉네임",
      "author_type": "member",
      "author_ip": "",
      "date": "2026-01-31",
      "time": "14:30:25",
      "datetime": "2026-01-31T14:30:25",
      "views": 1234,
      "likes": 56,
      "comments": 23,
      "url": "https://gall.dcinside.com/mgallery/board/view/?id=thesingularity&no=12345"
    }
  ]
}
```

## 🚀 로컬 실행

### 1. 의존성 설치

```bash
pip install -r requirements.txt
```

### 2. 크롤러 실행

```bash
python crawl_dc.py
```

**첫 실행 시**: 최근 4주(28일)간의 데이터를 수집합니다.  
**이후 실행**: 새로운 게시글만 추가합니다 (중복 제거).

## ⚙️ GitHub Actions 자동화

### 설정 방법

1. GitHub 저장소에 코드 푸시
2. **Settings** → **Actions** → **General** → **Workflow permissions** → **Read and write permissions** 활성화
3. 자동 실행 시작!

### 실행 스케줄

- **자동 실행**: 매 6시간마다 (UTC 기준 00:00, 06:00, 12:00, 18:00)
- **수동 실행**: GitHub Actions 탭에서 "Run workflow" 버튼 클릭

### 실행 내역 확인

1. GitHub 저장소의 **Actions** 탭 이동
2. "DC Crawler Auto Run" 워크플로우 클릭
3. 각 실행 로그 확인 가능

## 📈 데이터 활용 예시

### Python에서 사용

```python
import json

# 주차별 데이터 로드
with open('data/2026_W05.json', 'r', encoding='utf-8') as f:
    data = json.load(f)

# 게시글 순회
for post in data['posts']:
    print(f"{post['title']} - {post['author']} ({post['datetime']})")
    print(f"조회: {post['views']}, 추천: {post['likes']}, 댓글: {post['comments']}")
```

### 통계 분석

```python
# 가장 많이 조회된 게시글
top_viewed = sorted(data['posts'], key=lambda x: x['views'], reverse=True)[:10]

# 가장 활발한 작성자
from collections import Counter
author_counts = Counter(post['author'] for post in data['posts'])
top_authors = author_counts.most_common(5)
```

## 🔧 설정 변경

`crawl_dc.py` 상단의 설정값을 수정할 수 있습니다:

```python
GALL_ID = "thesingularity"  # 갤러리 ID
DAYS_LIMIT = 28             # 수집 기간 (일)
```

다른 디씨인사이드 갤러리를 크롤링하려면 `GALL_ID`와 `GALL_NAME`을 변경하세요.

## ⚠️ 주의사항

- **디씨인사이드 서버 부하**: 페이지당 0.5초 딜레이 적용
- **GitHub Actions 무료 한도**: 월 2,000분 (현재 설정으로 약 120분 사용 예상)
- **데이터 크기**: 주당 약 100KB, 연간 약 5MB 예상

## 📝 라이선스

MIT License

## 🤝 기여

이슈와 Pull Request를 환영합니다!
